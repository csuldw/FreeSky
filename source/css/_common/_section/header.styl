.headband {
  height: 3px;
  background: $headband-bg;
}

.header { border:1px solid #ECECEC; z-index:999;position: relative; }

.header-inner { position: relative; z-index:8888; padding: 40px 0 20px;}

.site-meta {
  margin: 0;
  text-align: $site-meta-text-align;

  +mobile() { text-align: center; }
}

.brand {
  position: relative;
  display: inline-block;
  padding: 0 40px;
  color: #797474
  border-bottom: none;
  font-family: $brand-font-family;
}

.logo {
  display: inline-block;
  margin-right: 5px;
  line-height: 36px;
  vertical-align: top;
}

.site-title {
  display: inline-block;
  vertical-align: top;
  line-height: 36px;
  font-size: 20px;
  font-weight: normal;
}

.site-subtitle {
  margin-top: 10px;
  font-size: 13px;
  color: $grey-dark;
}

.use-motion {
  .brand { opacity: 0; }

  .logo, .site-title, .site-subtitle {
    opacity: 0;
    position: relative;
    top: -10px;
  }
}


.site-nav-toggle {
  display: none;
  position: absolute;
  top: 10px;
  left: 10px;
  +mobile() {
    display: block;
  }

  button {
    margin-top: 2px;
    padding: 9px 10px;
    background: transparent;
    border: none;
  }
}

.site-nav {
  +mobile() {
    display: none;
    margin: 0 -10px;
    padding: 0 10px;
    clear: both;
    border-top: 1px solid $gray-lighter;
    z-index:9999
  }
  +tablet() { display: block !important; }
  +desktop() { display: block !important; }
}


// Menu
// --------------------------------------------------
.menu {
  margin-top: 48px;
  padding-left: 33px;
  text-align: left;
  color: $blue-deep;
}

.menu .menu-item {
  display: inline-block;
  margin: 0 10px;
  @media screen and (max-width: 767px) {
    margin-top: 10px;
  }

  a {
    display: block;
    font-size: 14px;
    text-transform: capitalize;
    line-height: 1.5;
    border-bottom: 1px solid $menu-link-border;
    transition-property: border-color;
    the-transition();

    &:hover { border-bottom-color: $menu-link-border; }
  }
}

.use-motion .menu-item { opacity: 0; }
